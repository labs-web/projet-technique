# Projet Technique - Installation de Stack

## 🎯 Philosophie d'Installation

Ce projet privilégie une approche **"Installation Manuelle Guidée par IA"**. 
L'objectif est que le développeur reste maître de son environnement tout en étant assisté pour la configuration et la validation.

L'agent **ne force pas** l'installation de composants déjà présents mais **vérifie** leur conformité et **complète** la configuration (ex: ajout de Preline UI sur une installation Tailwind existante).

---

## 🛠️ Prérequis Système

Avant de commencer, assurez-vous d'avoir :

- **PHP** : 8.2+ (Extensions courantes requises)
- **Composer** : Dernière version stable
- **Node.js** : Version LTS (20.x recommandé)
- **npm** : Version correspondante à Node.js

---

## 🚀 Workflow d'Installation

Utilisez le workflow interactif pour configurer votre stack :

```bash
# Lancer l'assistant de configuration
@[/installation-stack] 
```

Vous pouvez spécifier les composants à vérifier/installer :

```bash
# Vérifier/Installer tout le socle
@[/installation-stack] laravel tailwind alpine architecture

# Juste configurer le CSS
@[/installation-stack] tailwind
```

### Technologies Gérées par l'Assistant

| Composant        | Action de l'Assistant                                            |
| :--------------- | :--------------------------------------------------------------- |
| **Laravel**      | Affiche la commande `create-project` pour installation manuelle. |
| **Tailwind CSS** | Vérifie l'existant. Ajoute **Preline UI** si manquant.           |
| **Alpine.js**    | Installe et configure `app.js` si absent.                        |
| **Architecture** | Crée l'arborescence recommandée (Services, Policies...).         |

---

## 📚 Guide d'Installation Manuelle

Si vous procédez sans l'assistant, suivez ces étapes :

### 1. Installation de Laravel (Socle)

```bash
# Dans le dossier racine du projet
composer create-project laravel/laravel:^11.0 app
cd app
```

*Note : Laravel 11+ installe automatiquement une configuration de base pour Tailwind (ou la prépare).*

### 2. Configuration Tailwind CSS + Preline UI

Si Tailwind est déjà là, ajoutez simplement Preline UI pour les composants :

```bash
npm install preline --save-dev
```

Mise à jour de `tailwind.config.js` (ou équivalent) :
```javascript
export default {
    content: [
        // ... (chemins existants)
        'node_modules/preline/dist/*.js',
    ],
    plugins: [
        require('preline/plugin'),
    ],
}
```

Mise à jour de `resources/js/app.js` :
```javascript
import 'preline';
```

### 3. Installation Alpine.js

```bash
npm install alpinejs
```
Initialisation dans `app.js` (si non automatique) :
```javascript
import Alpine from 'alpinejs';
window.Alpine = Alpine;
Alpine.start();
```

---

## ✅ Validation de l'Installation

Pour vérifier que votre stack est opérationnelle :

1.  **Lancer le serveur Laravel** : `php artisan serve`
2.  **Compiler les assets** : `npm run dev`
3.  **Vérifier** :
    *   La page d'accueil s'affiche sur `http://localhost:8000`.
    *   La console navigateur ne montre pas d'erreurs JS.
    *   Les classes utilitaires Tailwind fonctionnent.
